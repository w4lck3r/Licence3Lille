#include <unistd.h>
#include <stdio.h>
#include <pthread.h>
#include <assert.h>
#include <stdlib.h>

int fibo (){

}


int main(){
 int n, res;
    for (int i=0; i<n ;i++){
        fibtab[i] = INCONNUE;
    }
    n = strtol(argv[1], NULL, 10); 
    res = fib(n);
    printf("fib(%d)=%d\n", n, res);
    

    exit(EXIT_SUCCESS);
}